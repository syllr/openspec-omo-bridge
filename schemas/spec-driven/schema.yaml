name: spec-driven
version: 1
description: Default OpenSpec workflow - proposal → design → specs → tasks → critic → apply
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      __LANG_PLACEHOLDER__

      **范围限制：不要修改任何源代码。仅生成 proposal.md。**

      === PHASE 1：上下文提取 ===

      在编写 proposal 之前，系统性地回顾当前对话，提取所有与本变更相关的信息。

      **如果对话超过 30 条消息（用户 + AI 的交流）：**
      1. 将对话分为三段：前期、中期、后期
      2. 从每段中分别提取关键点（每段 3-8 条）
      3. 合并所有段落的要点，去重并综合
      4. 特别注意中期段落中出现但后续未被重提的点
      5. 标记在任何段落中发现的不确定信号

      **对于较短的对话：** 直接从完整对话中提取。

      将发现组织为 6 个类别：

      1. **核心动机**：什么驱动了这次讨论？用户的明确痛点是什么？
      2. **变更范围**：讨论到的每个具体能力、修改或移除项，包括提到的技术约束
      3. **显式约束**：用户明确的边界——"必须用 X"、"不能用 Y"、兼容性要求
      4. **显式非目标**：用户明确说不在范围内的事项
      5. **不确定信号**：用户说"需要调研"、"不确定"、"可能需要研究"的点，
         每个标记为 `[NEEDS INVESTIGATION]`
      6. **影响系统**：用户提到的代码、API、依赖、团队

      **这是思考步骤——不写入文件。**

      === PHASE 2：生成 ===

      读取模板文件 `schemas/spec-driven/templates/proposal.md`，了解确切的输出结构。

      在 `openspec/changes/<change-name>/proposal.md` 创建提案文档。

      按模板结构编写全部 4 个部分：
      - **Why**（动机）：1-2 句话说明问题或机会
      - **What Changes**（变更内容）：具体的变更项列表，不兼容变更标记 **BREAKING**
      - **Capabilities**（能力清单）：
        - New Capabilities（新能力）：kebab-case 命名，每个对应一个 spec 文件
        - Modified Capabilities（修改的能力）：检查 `openspec/specs/` 中已有的名称
      - **Impact**（影响）：受影响的代码、API、依赖、系统

      **完整性优先**——覆盖 PHASE 1 中的所有要点。保持语言简洁，但不以覆盖度为代价。

      提案应让技术和非技术读者都能理解。

      编写后验证输出是否完全遵循模板结构。

      === PHASE 3：Oracle + Metis 并行审查（强制） ===

      同时启动 1 个 Oracle + 1 个 Metis agent 审查生成的 proposal。
      调用前将占位符替换为实际内容。

       **Oracle prompt 和 Metis prompt 使用相同内容：**
       task(subagent_type="oracle", run_in_background=false, prompt="
       你正在审查一个 OpenSpec 变更提案。请逐一检查以下所有检查项：

       PHASE 1 的上下文提取结果：
       [将 PHASE 1 的提取输出粘贴此处]

       生成的 proposal.md 路径：openspec/changes/<change-name>/proposal.md

       □ 内容完整性——PHASE 1 提取的所有要点是否在 proposal 中体现？
       □ 范围控制——提案中是否有对话中未讨论的内容（范围蔓延）？
       □ 结构完整性——所有必需的部分是否存在且格式正确？
       □ 格式卫生——有无空部分、未替换占位符、[TODO]/[TBD] 标记？
       □ 命名规范——Capabilities 是否使用 kebab-case？
       □ 边界清晰——What Changes / Capabilities / Impact 是否没有混淆？
       □ 可操作性——Capabilities 是否具体到可以衍生 specs？Impact 是否明确？
       □ 模糊性——表达是否清晰？有无 AI-slop（模糊空洞的表述）？
       □ 不确定信号——需要调研的点是否已用 [NEEDS INVESTIGATION] 标记？

       严重程度：🔴 阻塞 / 🟡 警告 / ⚪ 建议
       输出格式：按严重程度列出发现，对每个 🔴/🟡 给出具体修改建议
       ")

       task(subagent_type="metis", run_in_background=false, prompt="
        你正在审查一个 OpenSpec 变更提案。请逐一检查以下所有检查项：

        PHASE 1 的上下文提取结果：
        [将 PHASE 1 的提取输出粘贴此处]

        生成的 proposal.md 路径：openspec/changes/<change-name>/proposal.md

        □ 内容完整性——PHASE 1 提取的所有要点是否在 proposal 中体现？
        □ 范围控制——提案中是否有对话中未讨论的内容（范围蔓延）？
        □ 结构完整性——所有必需的部分是否存在且格式正确？
        □ 格式卫生——有无空部分、未替换占位符、[TODO]/[TBD] 标记？
        □ 命名规范——Capabilities 是否使用 kebab-case？
        □ 边界清晰——What Changes / Capabilities / Impact 是否没有混淆？
        □ 可操作性——Capabilities 是否具体到可以衍生 specs？Impact 是否明确？
        □ 模糊性——表达是否清晰？有无 AI-slop（模糊空洞的表述）？
        □ 不确定信号——需要调研的点是否已用 [NEEDS INVESTIGATION] 标记？

        严重程度：🔴 阻塞 / 🟡 警告 / ⚪ 建议
        输出格式：按严重程度列出发现，对每个 🔴/🟡 给出具体修改建议
        ")

      **收集评审结果：**
      1. 读取 Oracle 输出 → 列出所有 🔴/🟡/⚪ 发现
      2. 读取 Metis 输出 → 列出所有 🔴/🟡/⚪ 发现
      3. **自检**：确认两个 agent 的结果都已成功读取。如有遗漏，回到步骤 1 重新读取
      4. 合并发现：同一问题多个 agent 都发现时取更高严重程度
      5. 分类判定：
         - 有任何 🔴 → BLOCKED，修复 proposal.md 后重新运行 PHASE 3（最多 3 轮）
         - 仅 🟡/⚪ → 展示给用户，让用户评估决定是否修复

      **Fast fail：** 如果任一 agent 调用失败（超时、不可用、出错）：
      - 立即停止工作流
      - 向用户报告：「❌ proposal 审查中断：[agent name] 调用失败。[error message]」
      - 不得降级，不得自行修复，不得跳过审查
      - 等待用户介入

       === PHASE 4：循环与继续 ===

       - 如果在 PHASE 3 中发现 🔴：修复 proposal.md，从 PHASE 3 重新运行
       - 最多 3 轮。3 轮后仍有 🔴：将剩余问题展示给用户
       - 全部清除：proposal 完成，进入下一个 artifact
    requires: []
  - id: design
    generates: design.md
    description: Technical design document with implementation details (generated before specs)
    template: design.md
    instruction: |
      __LANG_PLACEHOLDER__

      **范围限制：不要修改任何源代码。仅生成 design.md。**

      === PHASE 1：读取输入 ===

      读取以下文件提取设计所需信息：
      - `openspec/changes/<change-name>/proposal.md` — 动机和能力

      === PHASE 2：技术调研（可选） ===

      阅读 proposal 时，注意以下**不确定性信号**：
      - "可能"、"maybe"、"might"、"needs investigation" 等词语
      - 引用未文档化的 API 或协议
      - 关于 API 字段、签名或认证方式的问题
      - 不明确的库行为或兼容性问题

      对于每个不确定点，按以下流程处理：

       1. **识别调研主题**：从 proposal.md 中提取每个不确定点，命名成 kebab-case 的调研主题。例如：`api-signature-format`、`plugin-config-schema`

       2. **两波并发调研**：

          所有调研产物（脚本、文档、API 响应）保存到临时目录 `$project/spectest/<topic>/`。用户确认后移动到 `openspec/changes/<change-name>/research/<topic>/`

          **第一波 — 所有 librarian 并发**：在同一条消息中发起所有主题的 librarian 任务。每个 librarian 调研其主题的文档或源代码。

          ```
          task(subagent_type="librarian", run_in_background=false, prompt="调研 plugin.xml 配置声明格式...")
          task(subagent_type="librarian", run_in_background=false, prompt="调研 OAuth2 PKCE 流程...")
          # ...N 个 topic，全部并发
          ```

          等待所有 librarian 完成后，将每个结果保存到 `spectest/<topic>/notes.md`

          **第二波 — 所有 deep agent 并发**：所有 librarian 完成后，在同一条消息中发起所有主题的分析任务。每个 deep agent 阅读 librarian 的调研结果，编写使用真实 API/接口调用的验证脚本。

          ```
          task(category="deep", load_skills=[], run_in_background=false, prompt="基于 spectest/plugin-config/notes.md 的调研结果，
          写一个脚本来实际测试 plugin.xml 配置声明格式。调用真实接口验证签名方式，
          给出完整的代码示例和调用方式。将脚本和结果保存到 spectest/plugin-config/。")
          task(category="deep", load_skills=[], run_in_background=false, prompt="基于 spectest/pkce-flow/notes.md 的调研结果，
          写一个脚本来验证 PKCE code_challenge 生成流程...")
          # ...N 个 topic，全部并发
          ```

          等待所有 deep agent 完成后，将每个结果保存到 `spectest/<topic>/`

       3. **用户确认**：向用户展示所有调研结果的摘要。用户确认后，将每个 `spectest/<topic>/` 目录移动到 `openspec/changes/<change-name>/research/<topic>/`

          确认后的最终结构：

          ```
          openspec/changes/<change-name>/research/
          ├── index.md                    ← 所有调研点的摘要 + 最终结论
          ├── <topic-1>/                  ← 每个调研点一个子目录
          │   ├── notes.md               ← 调研笔记（文档摘录、分析）
          │   ├── test-script.sh/py/js   ← 验证脚本（可执行）
          │   └── results.json           ← 实际返回结果（接口响应等）
          └── <topic-2>/
              ├── notes.md
              └── ...
          ```

       **重要**：
       - research/ 目录是**可选的** — 只有 proposal 中存在不确定点时才创建
       - 验证脚本必须是**实际可运行的**（不是伪代码）
       - spectest/ 是临时目录，用户确认后数据才会移到 research/

      === PHASE 3：通过 Deep Agent 生成 ===

      打包所有上下文和研究发现（如果存在）到完整 prompt 中，委托 deep agent 生成设计文档：

      task(category="deep", load_skills=[], run_in_background=false,
           prompt="为以下变更生成技术设计文档（design.md）。

           输入文件（请直接读取以下文件获取完整内容）：
           - proposal.md：openspec/changes/<change-name>/proposal.md
           - research/：openspec/changes/<change-name>/research/（如果存在）

           设计文档必须包含以下部分：

           1. **Context**（上下文）：背景、当前状态、约束、干系人
           2. **Goals / Non-Goals**（目标/非目标）：设计要实现的和明确排除的
           3. **Decisions**（决策）：关键技术选择及理由（为什么选 X 不选 Y？）。包含考虑过的替代方案
           4. **Risks / Trade-offs**（风险/权衡）：已知限制和可能出错的地方。格式：[风险] → 缓解措施
           5. **Migration Plan**（迁移计划）：部署步骤、回滚策略（如适用）
           6. **Open Questions**（未决问题）：尚未解决的决策或未知事项

           关键要求：
           - 关注架构和方法，而非逐行实现
           - 每个设计决策必须对应至少一个 proposal 中的 Capability
           - 如果存在 research findings，在相关 Decisions 中引用，用 `详见 research/<topic>/notes.md` 格式
           - 解释每个技术决策背后的"为什么"
           - 保持聚焦和可执行")

      生成后验证 design.md 是否覆盖所有必需部分。如果 research/ 存在，确认调研发现已在 Decisions 中被引用。
    requires:
      - proposal
  - id: specs
    generates: specs/**/*.md
    description: Detailed specifications for the change (generated after design)
    template: spec.md
    instruction: |
      __LANG_PLACEHOLDER__

      **范围限制：不要修改任何源代码。仅生成 spec 文件。**

      === PHASE 1：读取输入 ===

      读取以下文件，提取 spec 所需的关键信息：
      - `openspec/changes/<change-name>/proposal.md` — 动机和 Capabilities
      - `openspec/changes/<change-name>/design.md` — 技术决策和设计细节
      - `openspec/changes/<change-name>/research/` — 技术调研结论（如果存在）

      如果 proposal 没有列出任何 Capabilities（New 和 Modified 部分都为空），跳过 spec 文件生成。创建一个空的 `specs/` 目录并继续下一个 artifact。

      === PHASE 2：通过 Deep Agent 生成 ===

      将 proposal 和 design 的关键内容打包，委托 deep agent 生成 spec 文件：

      task(category="deep", load_skills=[], run_in_background=false,
           prompt="根据以下输入生成 spec 文件。

           输入文件（请直接读取以下文件获取完整内容）：
           - Proposal：openspec/changes/<change-name>/proposal.md
           - Design：openspec/changes/<change-name>/design.md
           - Research：openspec/changes/<change-name>/research/（如果存在）

           为 proposal 的 Capabilities 部分列出的每个能力创建一个 spec 文件。
           - 新能力：使用 proposal 中确切的 kebab-case 名称（specs/<capability>/spec.md）
           - 修改的能力：使用 openspec/specs/<capability> 中已有的 spec 文件夹名称

           VALIDATION RULES（openspec validate 强制检查以下规则）：
           1. 每个 requirement 必须以 `### Requirement: <name>` 开头
           2. Requirement 描述中必须包含大写 MUST 或 SHALL，不要用中文"必须"
              - 肯定式："The system MUST ..."
              - 否定式："The system MUST NOT ..."
           3. 每个 requirement 必须至少有一个 scenario
           4. 每个 scenario 必须以 `#### Scenario: <name>` 开头（恰好 4 个 #）
           5. 每个 scenario 正文必须包含 WHEN / THEN 关键词

           Delta 操作（使用 ## 标题）：
           - **ADDED Requirements**（新增需求）：新能力
           - **MODIFIED Requirements**（修改的需求）：行为变更，必须包含完整更新内容
           - **REMOVED Requirements**（移除的需求）：已废弃功能，必须包含**原因**和**迁移方案**
           - **RENAMED Requirements**（重命名的需求）：仅名称变更，使用 FROM:/TO: 格式

           MODIFIED requirements 工作流：
           1. 在 openspec/specs/<capability>/spec.md 中找到现有 requirement
           2. 复制完整的 requirement 块（从 ### Requirement: 到所有 scenarios）
           3. 粘贴到 ## MODIFIED Requirements 下，编辑以反映新行为
           4. 确保标题文本精确匹配

           常见陷阱：使用 MODIFIED 但内容部分丢失，会在归档时丢失细节。
           如果添加不改变现有行为的新关注点，请使用 ADDED。

           Spec 应该是可测试的 —— 每个 scenario 都是一个潜在的测试用例。

           Example 格式参考：
           ```
           ## ADDED Requirements
           ### Requirement: User can export data
           The system SHALL allow users to export their data in CSV format.
           #### Scenario: Successful export
           - **WHEN** user clicks "Export" button
           - **THEN** system downloads a CSV file with all user data
           ```")

      === PHASE 3：验证 ===

      编写完所有 spec 文件后，运行：
      `openspec validate <change-name>`
      修复所有 ERROR 后再继续。
    requires:
      - proposal
      - design
  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: |
      __LANG_PLACEHOLDER__

      **范围限制：不要修改任何源代码。仅生成 tasks.md。**

      创建任务列表（tasks.md）。

      **你必须创建一个文件：**

      `openspec/changes/<change-name>/tasks.md` — 标准 checkbox 任务清单

      === PHASE 1：读取输入 ===

      读取以下文件，提取生成 tasks.md 所需的关键信息：
      - `openspec/changes/<change-name>/proposal.md` — 动机和能力
      - `openspec/changes/<change-name>/specs/` — 需求和验收场景
      - `openspec/changes/<change-name>/design.md` — 技术方案、决策、风险
      - `openspec/changes/<change-name>/research/` — 技术调研（如果存在）


      **Step 1：快速 artifact 审查**

      生成前，快速检查：
      1. 每个 proposal 的 Capability 是否都有对应的 spec？
      2. task 是否覆盖了所有 spec 的关键 Requirement？
      3. 有无矛盾？
      → 如果发现问题：停止，列出具体问题，等待用户处理。
      → 如果全部正常：继续。

      **Step 2：Oracle + Metis 并行差距分析（强制）**

      调用 Oracle 和 Metis 同时做差距分析，两者使用相同的审查维度。调用前先读取所有 artifacts。

      **Fast fail 保护**: 如果任一 agent 调用失败（超时、不可用、出错），立即停止工作流（**fast fail，不得降级**）：
      - 向用户报告：「❌ 差距分析中断：[agent name] 调用失败。[错误信息]」
      - 不得自行分析 gap，不得自行生成 tasks.md
      - 等待用户介入

      TASKS_GAP_PROMPT = "你正在审查一个 OpenSpec change 的产物，为生成 tasks.md 做准备。
      请逐一检查以下所有检查项：

      审查文件：
      - proposal：openspec/changes/<change-name>/proposal.md
      - specs：openspec/changes/<change-name>/specs/
      - design：openspec/changes/<change-name>/design.md
      - research：openspec/changes/<change-name>/research/（如果存在）

      信源优先级：proposal.md > design.md > specs

      □ 遗漏与溢出——proposal 中的要点未被 specs/design 覆盖？反之，specs/design 中是否有 proposal 未提及的内容（scope leak）？
      □ scope creep / AI-slop——是否有模糊、空洞或不必要的范围扩张？
      □ 缺失的验收标准——每个 requirement 是否有明确的验收方式？
      □ 未覆盖的边界情况——错误路径、并发、权限等是否被考虑？
      □ artifacts 一致性——specs 和 design 之间是否有矛盾？（spec 说 A，design 说 B）
      □ 表达模糊——是否有表述不清、可能产生歧义的地方？
      □ task 可衍生性——每个 requirement 是否能自然拆分为一个或多个可执行的 task？
      □ 依赖隐含——是否有隐含的先后依赖关系未被明确声明？

      严重程度：🔴 阻塞 / 🟡 警告 / ⚪ 建议
      输出格式：按严重程度列出发现，对每个 🔴/🟡 给出具体修改建议"

      task(subagent_type="oracle", run_in_background=false, prompt=TASKS_GAP_PROMPT)
      task(subagent_type="metis", run_in_background=false, prompt=TASKS_GAP_PROMPT)

      **收集评审结果：**
      1. 读取 Oracle 输出 → 列出所有 🔴/🟡/⚪ 发现
      2. 读取 Metis 输出 → 列出所有 🔴/🟡/⚪ 发现
      3. **自检**：确认两个 agent 的结果都已成功读取。如有遗漏，回到步骤 1 重新读取
      4. 合并发现：同一问题多个 agent 都发现时取更高严重程度
      5. 分类判定：
         - 有任何 🔴 → BLOCKED
           **说明**：此循环是内容质量改进机制（审查 agent 已成功完成分析 → subagent 修复 → 重新审查），
           不是 agent 调用失败的降级处理。如审查 agent 调用本身失败 → 按 fast fail 规则立即停住报错。
           将所有 🔴 问题打包，调用 deep agent 一次性修复：
           task(category="deep", load_skills=[], run_in_background=false,
                prompt="根据以下差距分析结果，一次性修复所有被指出的问题。

                只修复标记为 🔴 的问题，不要修改其他内容。

               Oracle + Metis 审查发现的所有问题：
               [将 Oracle/Metis 输出的所有 🔴 问题及修复建议以文本形式填入此处]")
           修复完成后重新运行审查（**最多 3 轮**，超限后汇总给用户）
         - 仅 🟡/⚪ → 展示给用户，让用户评估决定是否修复


       === PHASE 2：通过 Deep Agent 生成 ===

        将所有 artifacts 的文件路径传给 deep agent，由其直接读取后生成 tasks.md：

       task(category="deep", load_skills=[], run_in_background=false,
            prompt="为以下变更生成任务清单（tasks.md）。

            输入文件（请直接读取以下文件获取完整内容）：
            - Proposal：openspec/changes/<change-name>/proposal.md
            - Specs：openspec/changes/<change-name>/specs/
            - Design：openspec/changes/<change-name>/design.md
            - Research：openspec/changes/<change-name>/research/（如果存在）

            任务清单必须：
            - 使用 `## Tasks` 作为部分标题
            - 使用 `### Wave N: Group Name` 子分组，每个 Wave 5-8 个 task，不少于 3 个
            - 每个 task 以 `- [ ] N.M Description` 开头

            每个 task 必须包含以下 9 个子字段：
            - **What to do**（做什么）：可执行的实施步骤
            - **Must NOT do**（不做什么）：明确的排除项/守卫栏
            - **Recommended Agent Profile**（推荐 agent）：category + skills
            - **References**（参考）：代码模式参考（文件路径+行号）
            - **Acceptance Criteria**（验收标准）：可执行的验证命令（如 `bun test`、`curl`）
            - **QA Scenarios**（QA 场景）：至少 1 个快乐路径 + 1 个失败/边缘路径，每个场景包含五要素（Tool / Preconditions / Steps / Expected / Evidence）
            - **Parallelization**（并行）：并行组、阻塞关系
            - **Evidence**（证据）：验证证据路径（.sisyphus/evidence/）
            - **Commit**（提交）：是否提交及提交信息

            Wave 规则：
            - 同一 Wave 内 task 默认并发执行
            - 不同 Wave 之间串行
            - 只有两种情况下需要串行：Input dependency（Task B 读 Task A 的输出）或 File conflict（两个 task 修改同一文件）
            - 没有文件读写关系的 task 不应有依赖关系

            覆盖所有 spec requirement 和 design decisions，按逻辑依赖关系组织 wave。")
    requires:
      - specs
  - id: critic
    generates: critic.md
    description: >
      Plan generation and multi-agent review. Generates
      .sisyphus/plans/<change-name>.md (OMO plan) and critic.md
      (review report with verdict).
    template: critic.md
    instruction: |
      __LANG_PLACEHOLDER__

      **范围限制：不要修改任何源代码。仅生成 critic.md 和 .sisyphus/plans/<change-name>.md。**

      === PHASE 1：读取上下文 ===

      读取以下文件：
      - `openspec/changes/<change-name>/tasks.md` — checkbox 任务清单
      - `openspec/changes/<change-name>/specs/` — 需求和验收场景
      - `openspec/changes/<change-name>/design.md` — 技术方案、决策、风险
      - `openspec/changes/<change-name>/proposal.md` — 动机和能力
      - `openspec/changes/<change-name>/research/` — 技术调研（如果存在）

      === PHASE 2：Spec 验证（强制） ===

      运行 `openspec validate <change-name>` 检查 spec 格式。

      如果验证通过 → 继续。
      如果验证失败 → 显示错误信息给用户，由用户决定如何修复。不得自行修复格式问题。

      === PHASE 3：Oracle + Metis 并行审查 tasks.md ===

      调用 Oracle 和 Metis 同时审查 tasks.md，两者使用相同的审查维度。

      **Fast fail 保护**: 如果任一 agent 调用失败（超时、不可用、出错），立即停止工作流（**fast fail，不得降级**）：
      - 向用户报告：「❌ tasks.md 审查中断：[agent name] 调用失败。[错误信息]」
      - 不得自行审查，不得跳过审查
      - 等待用户介入

      ```
      REVIEW_PROMPT = "你正在审查一个 OpenSpec change 的任务清单（tasks.md）。请逐一检查以下所有检查项：

       审查文件：
        - tasks:    openspec/changes/<change-name>/tasks.md
       - proposal: openspec/changes/<change-name>/proposal.md
       - specs:    openspec/changes/<change-name>/specs/
       - design:   openspec/changes/<change-name>/design.md（如果存在）
       - research: openspec/changes/<change-name>/research/（如果存在）

       □ 覆盖与 Scope——所有 spec/design 决策是否有对应 task 覆盖？有无多余 task（超出 proposal scope）或遗漏？
       □ Task 粒度——每个 task 是否粒度合理？有无拆分过粗或过细？
       □ 9 字段完整性与质量——每个 task 是否包含 What to do / Must NOT do / Agent Profile /
         References / Acceptance Criteria / QA Scenarios / Parallelization / Evidence / Commit？
         每个字段是否填写了有意义的内容（非占位符/非空白）？
       □ 任务可执行性——Acceptance Criteria 是否是具体可执行的命令？
         Agent Profile 的 category 是否匹配任务类型？
       □ QA Scenarios 质量——每个 QA Scenario 是否包含五要素（Tool/Preconditions/Steps/Expected/Evidence）？
         是否至少覆盖 1 个成功场景（happy path）+ 1 个异常场景？
       □ Wave 结构——Wave 分组是否合理？命名清晰？数量合理（5-8 个，不少于 3）？
       □ 并发依赖——同 Wave 内 task 是否互不依赖？不同 Wave 依赖是否真实？有无虚假依赖？
       □ 描述质量与 Evidence/Commit——task 描述是否有模糊、空洞、歧义？
         Evidence 是否可采集？Commit 信息是否描述 task 本质变更？

       严重程度：🔴 阻塞 / 🟡 警告 / ⚪ 建议
       输出格式：逐项检查，然后汇总问题清单，每个问题标注 severity。"

      task(subagent_type="oracle", run_in_background=false, prompt=REVIEW_PROMPT)
      task(subagent_type="metis", run_in_background=false, prompt=REVIEW_PROMPT)
      ```

      **收集评审结果（以下步骤不可跳过）**：
      1. 读取 Oracle 输出 → 列出所有 🔴/🟡/⚪ 发现
      2. 读取 Metis 输出 → 列出所有 🔴/🟡/⚪ 发现
      3. **自检**：确认两个 agent 结果都已读完。如有遗漏，回到步骤 1 重新读取
      4. 合并发现：同一问题多个 agent 都发现时取更高严重程度
      5. 分类判定：
         - 有任何 🔴 → BLOCKED
           **说明**: 此循环是内容质量改进机制（审查 agent 已成功完成分析 → subagent 修复 → 重新审查），
           不是 agent 调用失败的降级处理。如审查 agent 调用本身失败 → 按 fast fail 规则立即停住报错。
           将所有 🔴 问题打包，调用 deep agent 一次性修复：
           task(category="deep", load_skills=[], run_in_background=false,
                prompt="根据以下审查结果，一次性修复所有被指出的问题。

                只修复标记为 🔴 的问题，不要修改其他内容。

                Oracle + Metis 审查发现的所有问题：
                [将 Oracle/Metis 输出的所有 🔴 问题及修复建议以文本形式填入此处]")
           修复完成后重新运行审查（**最多 3 轮**，超限后汇总给用户）
         - 仅 🟡/⚪ → 展示给用户，让用户评估决定是否修复
       5. 产出合并清单：将合并后的发现清单写入输出，供后续 PHASE 引用

      === PHASE 4：生成计划 ===

      生成 `.sisyphus/plans/<change-name>.md` — 包含 9 个部分的 OMO 兼容执行计划。

      Delegate plan generation via category="deep":

      task(category="deep", load_skills=[], run_in_background=false,
           prompt="为以下变更生成 OMO 兼容的执行计划，保存到 .sisyphus/plans/<change-name>.md。

           输入文件摘要（已读取）：
           - Proposal: openspec/changes/<change-name>/proposal.md
           - Specs: openspec/changes/<change-name>/specs/
           - Design: openspec/changes/<change-name>/design.md
           - Tasks: openspec/changes/<change-name>/tasks.md
           - Research: openspec/changes/<change-name>/research/（如果存在）

           计划必须包含以下 9 个部分，使用 summary + link 格式：
           1. ## TL;DR — 1-2句概述（引用 proposal.md）
           2. ## Context — 2-3句背景（引用 proposal.md）
           3. ## Work Objectives — 每个 capability 及关键 requirement（引用 specs/）
           4. ## Verification Strategy — 引用 specs 的 scenarios
           5. ## Execution Strategy — 引用 design.md 和 research/
           6. ## Tasks — tasks.md 的 checkbox 展开，每个 task 含 What to do / Must NOT do / Agent Profile / References / Acceptance Criteria / QA Scenarios / Parallelization / Evidence / Commit
           7. ## Final Verification Wave — F1~F4
           8. ## Commit Strategy
           9. ## Success Criteria

           关键规则：
           - Task 编号必须与 tasks.md 一致（N.M 格式）
           - 不要复制 artifact 原文（Tasks 的 What to do 部分除外）
           - Acceptance Criteria 必须是可执行的命令")

      === PHASE 5：Plan 结构验证 ===

      编写 plan 后，执行以下验证：
      1. `grep -c '^## ' .sisyphus/plans/<change-name>.md` → 必须至少 9 个
      2. `grep -q '## Tasks' .sisyphus/plans/<change-name>.md` → Tasks 部分存在
      3. `grep -q '## Final Verification Wave' .sisyphus/plans/<change-name>.md` → FVW 存在
      4. `sed -n '/^## Tasks/,/^## /p' .sisyphus/plans/<change-name>.md | grep -c '^- \['` 应等于 `grep -c '^- \[' openspec/changes/<change-name>/tasks.md`

      如果任何检查不通过：停止，修复 plan，重新验证（**最多 3 轮**，超限后汇总给用户）。

      === PHASE 6：Oracle + Metis + Momus 并行审查 Plan ===

      同时启动 1 个 Oracle + 1 个 Metis + 1 个 Momus agent 并行审查生成的 plan。
      三个 agent 各自从内容完整性、结构格式、可执行性、潜在问题 4 个维度进行全面审查。
      Momus 额外负责最终门禁判定（OKAY/REJECT）。

      ```
       PLAN_REVIEW_PROMPT = "你正在审查 OMO plan。请逐一检查以下所有检查项：

        审查文件：
        - plan:  .sisyphus/plans/<change-name>.md
        - specs: openspec/changes/<change-name>/specs/
        - design: openspec/changes/<change-name>/design.md（如果存在）
        - tasks: openspec/changes/<change-name>/tasks.md
        - research: openspec/changes/<change-name>/research/（如果存在）

        □ 内容完整性——Execution Strategy 是否准确反映了 design 的决策？
          Verification Strategy 是否覆盖了所有 spec scenarios？
          Success Criteria 是否可衡量且覆盖所有 capability？
          research/ 的调研发现是否被正确引用？
        □ 结构格式——所有 9 个 section 是否存在？是否使用 summary + link 模式？
          section 间是否有逻辑连贯性？
          每个 task 的子字段是否齐全？QA Scenarios 是否有五要素？
        □ 可执行性——FVW 是否合理？每个 scenario 是否有明确验证方式？
          执行策略是否可行？Wave 分组合理？同 Wave 可并发？依赖真实？
        □ AI-slop——是否有遗漏的边界场景？逻辑漏洞？矛盾？
          QA Scenarios 是否至少覆盖 1 个快乐路径 + 1 个失败路径？

        严重程度：🔴 阻塞 / 🟡 警告 / ⚪ 建议
        输出格式：逐项检查，然后汇总问题清单，每个问题标注 severity。"

      ORACLE_PLAN_REVIEW_PROMPT = PLAN_REVIEW_PROMPT
      METIS_PLAN_REVIEW_PROMPT = PLAN_REVIEW_PROMPT

      MOMUS_PLAN_REVIEW_PROMPT = "你正在对 OMO plan 执行双重门禁审查（Momus 评审）。

      **Part A — 执行路径审查（可操作性）**：
      1. Wave 依赖是否合理？同一 Wave 内的 task 是否真的可以并发？
      2. 每个 task 的 Acceptance Criteria 是否可执行？（必须是具体的命令，如 `bun test`、`curl`）
      3. 有无明显的执行顺序错误？
      4. FVW 是否覆盖了关键验证点？
      5. 如果 research/ 存在：调研结论是否被 plan 正确引用？

      **Part B — 风险矩阵审查（遗漏覆盖）**：
      1. 是否有遗漏的边界场景（空值、错误路径、并发冲突、权限不足）？
      2. QA Scenarios 是否至少包含一个快乐路径 + 一个失败路径？
      3. Commit Strategy 是否合理？是否有多人协作的冲突风险？
      4. Success Criteria 是否真正可衡量？
      5. 如果 research/ 存在：是否有 research 发现未被 plan 覆盖？

      审查文件：
      - plan:  .sisyphus/plans/<change-name>.md
      - tasks: openspec/changes/<change-name>/tasks.md
      - research: openspec/changes/<change-name>/research/（如果存在）

      最终判定（二选一，综合 Part A 和 Part B）：
      - ✅ OKAY — plan 可执行，无 blocking issues
      - ❌ REJECT — plan 不可执行，需要修复后重新评审

      输出格式：先按 Part A / Part B 分别分析，最后给出综合判定。"

      # Oracle（内容）+ Metis（结构）+ Momus（门禁）
      task(subagent_type="oracle", run_in_background=false, prompt=ORACLE_PLAN_REVIEW_PROMPT)
      task(subagent_type="metis", run_in_background=false, prompt=METIS_PLAN_REVIEW_PROMPT)
      task(subagent_type="momus", run_in_background=false, prompt=MOMUS_PLAN_REVIEW_PROMPT)
      ```

      **收集评审结果（以下步骤不可跳过）**：

      1. **逐 agent 提取发现**：读取 Oracle 完整输出 → 读取 Metis 完整输出 → 读取 Momus 完整输出。每个 agent 单独列出发现。
      2. **自检**：确认所有 3 个 agent 结果都已读完。如有遗漏，回到 Step 1 重新读取。
      3. **合并去重**：按 severity 合并，标注来源（Oracle / Metis / Momus）。同一问题多个 agent 都发现时取更高 severity。
      4. **分类判定**：
          - Momus ❌ REJECT → 修复 plan 后重新运行 PHASE 6（**最多 3 轮**，超限后汇总给用户）
          - Oracle/Metis 有 🔴 → BLOCKED，修复 plan 后重新运行 PHASE 6（**最多 3 轮**，超限后汇总给用户）
          **说明**: 此循环是内容质量改进机制（审查 agent 已成功完成分析 → subagent 修复 → 重新审查），
           不是 agent 调用失败的降级处理。如审查 agent 调用本身失败 → 按 fast fail 规则立即停住报错。
          - 仅 🟡/⚪ → 评估后决定是否修复
       5. **产出合并清单**：将合并后的发现清单写入输出，供后续 PHASE 引用。

      === PHASE 7：通过 Deep Agent 生成 critic.md ===

      Package the complete review findings from PHASE 3 and PHASE 6 into a
      comprehensive prompt. Then delegate to a focused agent:

      task(category="deep", load_skills=[], run_in_background=false,
           prompt="Generate the critic.md file for this OpenSpec change.

           Review findings from PHASE 3 (tasks.md review by Oracle + Metis):
           [将 PHASE 3 的 Oracle 和 Metis 审查发现的 🔴/🟡/⚪ 清单填入此处]

           Review findings from PHASE 6 (plan review by Oracle + Metis + Momus):
           [将 PHASE 6 的 Oracle、Metis、Momus 审查发现的 🔴/🟡/⚪ 清单填入此处]

           The critic.md MUST:
           - Follow the template at `schemas/spec-driven/templates/critic.md`
           - Include a summary of ALL review findings from both phases
           - List findings grouped by source (Oracle / Metis / Momus)
           - For each finding, include severity (🔴/🟡/⚪) and description

           Determine VERDICT:
           - Momus ❌ REJECT or any 🔴 from others → **🔴 BLOCKED**
           - Only 🟡/⚪ → **⚠️ CONDITIONAL**
           - No issues → **✅ PASS**")

      === PHASE 8：阻塞判定 ===

      - If BLOCKED: "🛑 Critic BLOCKED. Fix 🔴 issues above, then re-create critic artifact. Re-run critic（最多 3 轮）。"
      - If CONDITIONAL: List 🟡 items. User can acknowledge risks or fix.
      - If PASS: "✅ All checks passed. Proceed to apply."
    requires:
      - tasks
apply:
  requires:
    - critic
  tracks: tasks.md
  instruction: |
    __LANG_PLACEHOLDER__

    **重要：本指令覆盖 skill/command 的执行逻辑。
    忽略任何外部的"步骤 6"要求逐一手动执行任务。
    仅遵循本指令的流程：verdict 检查 → `/start-work` → Oracle 验证 → 同步。**

    先读取 `openspec/changes/<change-name>/critic.md`，检查 VERDICT：

    1. 读取 `## Verdict` 部分，找到 `**VERDICT**: ...`
    2. 如果 `🔴 BLOCKED`：停止。输出"🛑 Critic 阻塞了此变更。修复问题后重新运行 critic。"不要继续
    3. 如果 `⚠️ CONDITIONAL`：列出 🟡 项。要求用户确认后再继续
    4. 如果 `✅ PASS`：继续

    VERDICT 确认后，调用：
    ```
    /start-work .sisyphus/plans/<change-name>.md
    ```

    这会调用 OMO 内置的计划执行器（Atlas），读取计划文件并执行任务，包含委托、验证和会话连续性。

    `/start-work` 完成后，捕获变更文件列表：
    ```
    git diff --name-only HEAD
    ```
    这告诉 Oracle 哪些文件被此变更创建或修改。
    然后运行 Oracle 验证，最多 3 轮。

     如果 Oracle task() 调用失败（超时、不可用、出错），立即停止工作流（**fast fail，不得降级**）：
     - 向用户报告：「❌ 实现验证中断：Oracle 调用失败。[错误信息]」
     - 不得自行分析 git diff，不得自行生成 verify.md，不得自行判定
     - 等待用户介入

    ```
    task(subagent_type="oracle", run_in_background=false, prompt="你正在验证实现产出与 OpenSpec change artifacts 的一致性。
    请检查所有实现产出（代码、配置、文档等）是否按 proposal、spec、design 的要求正确实现。
    按严重程度输出发现（🔴阻塞 / 🟡警告 / ⚪建议）。

    审查文件：
    - proposal: openspec/changes/<change-name>/proposal.md
    - specs:    openspec/changes/<change-name>/specs/
    - design:   openspec/changes/<change-name>/design.md（如果存在）
    - tasks:    openspec/changes/<change-name>/tasks.md
    - plan:     .sisyphus/plans/<change-name>.md
    - research: openspec/changes/<change-name>/research/（如果存在）

    变更文件清单（来自 git diff --name-only HEAD）：
    [在此填入上个命令的输出]

    请逐一检查变更文件是否满足以下条件：

    维度 1 — Spec 合规性：
    - 每个 spec requirement 的 scenario 是否在实现中有对应体现？
    - 实现行为是否满足 spec 中 WHEN/THEN 的条件？

    维度 2 — Design 对齐（实现与 Design 一致性）：
    - 实现方案是否符合 design.md 的技术决策？
    - design 中明确排除的 Non-Goals 是否在实现中出现？
    - research/ 中的调研结论是否在实现中得到正确应用？

    维度 3 — Proposal 范围：
    - 实现内容是否在 proposal 定义的 Capabilities 范围内？
    - 是否有超出 proposal 范围的 scope leak？
    - proposal 中的所有 Capability 是否都有对应实现？

    维度 4 — Task 完成度：
    - tasks.md 中的所有 task 是否都有对应的实现产出？
    - plan 中每个 task 的 Evidence 路径（.sisyphus/evidence/）是否存在且有效？

    输出格式：逐维度分析 + 问题汇总 + 总体结论。

    最终判定标准：
    - 🔴 BLOCKED：存在严重的实现偏离，必须修复才能继续
    - ⚠️ CONDITIONAL：实现基本正确但有 🟡/⚪ 瑕疵，可接受风险继续推进
    注意：请谨慎区分 BLOCKED 和 CONDITIONAL。只有确实阻碍功能正确性的问题才标 🔴。

    将完整结果保存到 openspec/changes/<change-name>/verify-round-{N}.md。")
    ```

    读取 `openspec/changes/<change-name>/verify-round-{N}.md` 中的最终判定：
    1. 如果 ✅ PASS → 通过，继续
     2. 如果 🔴 BLOCKED：
           读取 verify-round-{N}.md 中的 🔴 问题列表，打包所有问题，调用 deep agent 一次性修复：
           task(category="deep", load_skills=[], run_in_background=false,
                prompt="根据以下变更验证结果修复实现产出。只修复标注为 🔴 的问题，不要修改 🟡/⚪ 内容。

                Oracle 验证结果中的所有 🔴 问题：
                [将 verify-round-{N}.md 中的 🔴 问题列表及修复建议填入此处]")
          **修复策略**：
          - 每轮最多修复 3 个 🔴 问题，防止一次改动过大引入新问题
          - 修复前记录当前状态（git stash），以便失败后回滚
          - 修复后不要重新运行 /start-work，只修改产出文件
          修复完成后重新运行 Oracle 验证（**最多 3 轮**，超限后汇总给用户）
           **说明**: 此循环是内容质量改进机制（Oracle 已成功完成验证 → subagent 修复 → 重新验证），
           不是 agent 调用失败的降级处理。如 Oracle 调用本身失败 → 按 fast fail 规则立即停住报错。
    3. 如果 ⚠️ CONDITIONAL → 汇总 🟡/⚪ 问题展示给用户，让用户决定是接受继续还是修复后再重新运行 Oracle 验证

    **同步协议**：
    - plan 文件的 `## Tasks` 部分跟踪 `/start-work` 的 checkbox 状态
    - `openspec/changes/<change-name>/tasks.md` 是 OpenSpec 的真实来源
    - `/start-work` 完成或暂停后，将 `.sisyphus/plans/<change-name>.md` 的 checkbox 状态复制到
      `openspec/changes/<change-name>/tasks.md` 保持同步
    - 运行以下命令验证暂停/完成时的状态一致性：
      diff <(grep '^- \[' openspec/changes/<change-name>/tasks.md) <(sed -n '/^## Tasks/,/^## /p' .sisyphus/plans/<change-name>.md | grep '^- \[')
