## Why

<!-- 阐述本次变更的动机。这个变更解决了什么问题?为什么是现在? -->

## What Changes

<!-- 描述将会发生的变更。具体说明新增能力、修改或移除。 -->

## Capabilities

### New Capabilities

<!-- 新增的能力。将 <name> 替换为 kebab-case 标识符(如 user-auth、data-export、api-rate-limiting)。每个会生成 specs/<name>/spec.md -->

- `<name>`: <该能力覆盖内容的简要描述>

### Modified Capabilities

<!-- 现有能力的 REQUIREMENTS 正在变化(不仅是实现)。
     仅当 spec 层面行为变化时列出。每个需要 delta spec 文件。
     使用 openspec/specs/ 中现有的 spec 名。如果没有需求变化则留空。 -->

- `<existing-name>`: <正在变化的需求>

## Impact

<!-- 受影响的代码、API、依赖、系统 -->
